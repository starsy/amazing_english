<%
   url = "/events/#{params[:id]}/solutions/#{params[:solution_id]}"
%>

<%= bootstrap_form_for(@solution, url: url) do |f| %>
  <% if @solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solution.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% @solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.label :event_id %>: <%= params[:event_id] %><br>

    <%= f.text_area :text, size: "10x20" %>
    <%= f.text_field :provider %>

    <%= f.submit class: "btn btn-success" %>
  <% end %>
